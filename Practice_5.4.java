//Какие значения напечатает следующий код?
//Объясните полученный результат. Для чего используется класс IntegerCache?
//Вывод программы:
a1==i1 true
b1==i1 true
a1==b1 false
a1.equals(i1) -> true
b1.equals(i1) -> true
a1.equals(b1) -> true

a2==i2 true
b2==i2 true
a2==b2 true
a2.equals(i2) -> true
b2.equals(i2) -> true
a2.equals(b2) -> true
/*
Объяснение:
1. a1 == i1 → true
При сравнении Integer == int происходит автораспаковка: a1.intValue() == i1
Значения равны → true
2. a1 == b1 (для 128) → false
Обе переменные — объекты Integer
== сравнивает ссылки, а не значения
128 вне диапазона кэша → создаются два разных объекта
Ссылки разные → false
3. a2 == b2 (для 127) → true
127 входит в диапазон кэша [-128, 127]
Integer.valueOf(127) возвращает один и тот же объект из кэша
Ссылки совпадают → true
4. equals() всегда сравнивает значения → везде true

Зачем нужен IntegerCache?
Класс Integer содержит вложенный класс IntegerCache, который кэширует объекты Integer в диапазоне от -128 до 127.

Цель: экономия памяти и производительности (часто используемые малые числа)
При автоупаковке или вызове Integer.valueOf() для значений в этом диапазоне — возвращается кэшированный объект
За пределами диапазона — создаётся новый объект
*/