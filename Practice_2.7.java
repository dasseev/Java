//Какое значение будет в переменных b, с и d после выполнения данного кода.
int a = 120;
byte b = a + 10;          // ошибка
byte c = (byte)(a + 10);  // норм
byte d = a + 1;           // ошибка

//b — ошибка компиляции: результат a + 10 имеет тип int, нельзя неявно присвоить int переменной типа byte.
//c — успешно: явное приведение (byte) позволяет присвоить. Значение: (byte)130 = -126 (из-за переполнения: 130 - 256 = -126).
//d — ошибка компиляции: аналогично b.